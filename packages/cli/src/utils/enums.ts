// export enum InitLogMessage {
//   CSS_FILE_QUESTION = 'Where is the CSS file where Tailwind is imported?',
//   COMPONENTS_FOLDER_QUESTION = 'Which folder will the components be saved in?',
//   UTILS_FOLDER_QUESTION = 'Which folder will the utils file be saved in?',
//   CONFIGURE_PATH_ALIAS = "Now, let's configure path alias",
//   SETUP_SUCCESS = 'The setup was completed successfully!',
//   EXISTS_MANIFEST = 'The file `luxe.json` already exists!',
//   INVALID_PATH_ALIAS = 'Error! Enter a valid path format.',
// }

export enum Registry {
  COMPONENTS = 'registry:components',
}

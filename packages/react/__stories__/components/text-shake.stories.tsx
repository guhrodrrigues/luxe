import type { Meta, StoryObj } from '@storybook/react'

import { TextShake } from '@/registry/components/text-shake'

const meta: Meta = {
  title: 'components/TextShake',
  component: TextShake,
  parameters: {
    layout: 'centered',
  },
}

export default meta

export const Basic: StoryObj = {}

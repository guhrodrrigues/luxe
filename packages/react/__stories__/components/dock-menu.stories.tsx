import type { Meta, StoryObj } from '@storybook/react'

import { DockMenu } from '@/registry/components/dock-menu'

const meta: Meta = {
  title: 'components/DockMenu',
  component: DockMenu,
  parameters: {
    layout: 'centered',
  },
}

export default meta

export const Basic: StoryObj = {}

import type { Meta, StoryObj } from '@storybook/react'

import { ButtonBackgroundShine } from '@/registry/components/button-background-shine'

const meta: Meta = {
  title: 'components/ButtonBackgroundShine',
  component: ButtonBackgroundShine,
  parameters: {
    layout: 'centered',
  },
}

export default meta

export const Basic: StoryObj = {}

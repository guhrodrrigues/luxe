import type { Meta, StoryObj } from '@storybook/react'

import { CardBackgroundShine } from '@/registry/components/card-background-shine'

const meta: Meta = {
  title: 'components/CardBackgroundShine',
  component: CardBackgroundShine,
  parameters: {
    layout: 'centered',
  },
}

export default meta

export const Basic: StoryObj = {}
